// import {CompilerFinalResult} from '../src/parser';
// import {asm} from '../src/asm';

import './utils/binaryMatcher';

describe('equ', () => {
  // let compileResult: CompilerFinalResult = null;

  it('name already reserved', () => {});

  it('name already defined', () => {});

  // it('defines fixed digit', () => {
  //   compileResult = asm`
  //     mov ax, test2
  //     test2 equ 0x12FF
  //   `;

  //   expect(compileResult.output).toOutputsBinary(
  //     {
  //       0x0: 0x0,
  //     },
  //   );
  // });
});
